apiVersion: openchoreo.dev/v1alpha1
metadata:
  name: ceramics-webapp

endpoints:
  - name: http
    type: HTTP
    port: 8080
    basePath: /
    visibility:
      - external

dependencies:
  endpoints:
    - component: q11-ceramics-api
      name: http
      visibility: project
      envBindings:
        address: CERAMICS_API_URL
  resources:
    - ref: q11-user-auth
      envBindings:
        client_id: USER_AUTH_CLIENT_ID
        issuer: USER_AUTH_ISSUER
        jwks_url: USER_AUTH_JWKS_URL
        scopes: USER_AUTH_SCOPES
